language: bash

addons:
  apt:
    packages:
    - build-essential
before_install:
- sudo apt-get update -qq && sudo apt-get upgrade -qq zsh
- sudo add-apt-repository ppa:duggan/bats --yes
- sudo apt-get update -qq
- sudo apt-get install -qq bats


script:
    - bats tests.bats
